
@helper LinkButton(string id = "", string url = "", string color = "", string addtionalClass = "", string title = "", string icon = "", string text = "")
{
    <a href="@url" id="@id" name="@id" class="btn btn-@color @addtionalClass" title="@title">
        <i class="fa fa-@icon"></i>  @text
        <i class="fa fa-trash"></i>
    </a>
}
@helper LinkButton(string url = "", string color = "", string addtionalClass = "", string title = "", string icon = "", string text = "")
{
    <a href="@url" class="btn btn-@color @addtionalClass" title="@title">
        <i class="fa fa-@icon"></i>   @text
    </a>
}
@helper Button(string id = "", string color = "", string addtionalClass = "", string type = "button", string icon = "", string text = "")
{
    <button id="@id" name="@id" type="@type" class="btn btn-@color @addtionalClass">
        <i class="fa fa-@icon"></i> @text
    </button>
}
@helper Button(string color = "", string addtionalClass = "", string type = "button", string icon = "", string text = "")
{
    <button type="@type" class="btn btn-@color @addtionalClass">
        <i class="fa fa-@icon"></i> @text
    </button>
}
@helper LinkMenu(string url = "", string icon = "", string text = "")
{
    <a class="nav-link" href="@url">
        <i class="fa fa-@icon @(string.IsNullOrEmpty(text) ? "" :"mr-2")">
        </i>@text
    </a>
}
@helper DropdownLinkMenu(string url = "", string icon = "", string text = "")
{

    <a class="dropdown-item" href="@url">
        <i class="fa fa-@icon @(string.IsNullOrEmpty(text) ? "" :"mr-2")">
        </i>@text
    </a>
}
@helper DropdownDivider()
{
    <div class="dropdown-divider"></div>
}
@helper ModalPopup(string id="myModal",string title="Modal Title",Boolean showCloseButton=true)
{
    <!-- Modal -->
    <div class="modal fade" id="@id" name="@id" tabindex="-1" role="dialog" aria-labelledby="@(id+"_label")" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header" id="@(id+"_header")">
                    <h5 class="modal-title" id="@(id+"_label")">@title</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" id="@(id+"_body")">

                </div>
                <div class="modal-footer" id="@(id+"_footer")">
                    @if (showCloseButton)
                    {
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Kapat</button>
                    }
                </div>
            </div>
        </div>
    </div>

}